types_hash_max_size 2048;
types_hash_bucket_size 128;

# Configure nginx to allow larger uploads (10MB × 10 images + overhead = 120MB)
# This allows users to upload up to 10 images of 10MB each
client_max_body_size 120M;
client_body_buffer_size 128k;